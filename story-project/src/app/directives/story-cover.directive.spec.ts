import { StoryCoverDirective } from './story-cover.directive';

describe('StoryCoverDirective', () => {
  it('should create an instance', () => {
    const directive = new StoryCoverDirective();
    expect(directive).toBeTruthy();
  });
});
